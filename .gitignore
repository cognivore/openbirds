# Personal Finch export — never commit
secret/

# Nix
result
result-*

# Koka build artifacts
.koka/
build/

# SwiftPM
.build/
.swiftpm/
Packages/
Package.resolved

# Xcode (generated by xcodegen — regenerate, don't commit)
*.xcodeproj/
*.xcworkspace/
DerivedData/
host/ios/Info.plist
# xcodegen leaves a *.bak when its sed/edit step rewrites in place.
*.yml.bak

# Bundled font binaries — fetched into Resources/fonts/ by `just
# fetch-fonts` from upstream OFL sources (see docs/notes/typography.md).
# Not committed; regenerable.
host/ios/Resources/fonts/

# macOS
.DS_Store

# Direnv
.direnv/
.envrc.local
